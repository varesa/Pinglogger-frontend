#!/bin/env bash

ADDRESS=3g-testi.dy.fi
INTERVAL=5

while (true)
do
  result=$(ping -c 1 $ADDRESS)
  ping=$(echo $result | sed -r 's|.*time=([0-9.]*) ms|\1|g' | sed -r 's| --- .*ms||g')
  echo $(date +%s) $ping
  sleep $INTERVAL
done
